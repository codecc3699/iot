const int lm_35 = A1;

void setup() {
  Serial.begin(9600);
}

void loop() {
  int adc_val;
  float temp_val;
  adc_val = analogRead(lm_35);
  temp_val = (adc_val * 4.88) / 10.0;
  Serial.print("Temperature=");
  Serial.print(temp_val);
  Serial.print("Degree Celsius\n");
  delay(1000);
}
